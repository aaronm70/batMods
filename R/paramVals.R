##parm vals
#
#gamma_1_Val <- params$gamma_1_Val # clearance rate I->S
#gamma_2_Val <- params$gamma_2_Val # clearance rate I->R
#sigma_1_Val <-params$sigma_1_Val # clearance rate E->S
#sigma_2_Val <- params$sigma_2_Val# clearance rate E->R
#mu_Val <- params$mu_Val # juvenile maturation rate
#mj_Val <- params$mj_Val # juvenile death rate
#m_Val <- params$m_Val # adult death rate
#omega_m_Val <- params$omega_m_Val # maternal antibody waning rate
#omega_2_Val <-params$omega_2_Val # immune waning rate
#epsilon_Val <- params$epsilon_Val # incubation/recurrance rate E->I
#kappa_Val <- params$kappa_Val# carrying capacity
#c_Val <- params$c_Val #birth pulse scalar
#s_Val <- params$s_Val #birth pulse synchronicity
#phi_Val <- params$phi_Val #birth pulse timing
#rho_Val <- params$rho_Val #latency I->E
#R0_Val <-params$R0_Val
#betaVer<- params$betaVer
#gammaVer<- params$gammaVer
#sigmaVer<- params$sigmaVer
#
#
#gamma_1 <- params$gamma_1_Val # clearance rate I->S
#gamma_2 <- params$gamma_2_Val # clearance rate I->R
#sigma_1 <-params$sigma_1_Val # clearance rate E->S
#sigma_2 <- params$sigma_2_Val# clearance rate E->R
#mu <- params$mu_Val # juvenile maturation rate
#mj <- params$mj_Val # juvenile death rate
#m <- params$m_Val # adult death rate
#omega_m <- params$omega_m_Val # maternal antibody waning rate
#omega_2 <-params$omega_2_Val # immune waning rate
#epsilon <- params$epsilon_Val # incubation/recurrance rate E->I
#kappa <- params$kappa_Val# carrying capacity
#c <- params$c_Val #birth pulse scalar
#s <- params$s_Val #birth pulse synchronicity
#phi <- params$phi_Val #birth pulse timing
#rho <- params$rho_Val #latency I->E
#R0 <-params$R0_Val
#
#R0 = (epsilon*beta_1*N+beta_2*(epsilon+sigma_1+sigma_2+m))/((epsilon+sigma_1+sigma_2+m)*(gamma_1+gamma_2+m+rho)-epsilon*rho)/(dt*4)
#
